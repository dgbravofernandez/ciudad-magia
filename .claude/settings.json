{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "git diff --name-only HEAD 2>/dev/null | grep -q 'supabase/migrations/' && echo '⚠️  Migraciones modificadas en supabase/migrations/. Recuerda APLICARLAS A MANO en el SQL Editor de Supabase (no hay pipeline) y luego: npm run db:types' || true"
          }
        ]
      }
    ]
  }
}
